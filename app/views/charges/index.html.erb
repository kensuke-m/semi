<% if notice %>
  <div class="alert alert-info" role="alert">
    <p id="notice"><%= notice %></p>
  </div>
<% end %>

<div class="page-header">
  <h1>担当一覧</h1>
</div>
<p class="lead">
  「教員，科目，定員」を定義します．
  レコードを新規作成するとそれに対応する募集レコードとシラバスが自動生成されます．
  レコードを削除するとそれに対応する募集レコードとシラバスも削除されます．
  <mark>ただし，内容の更新には追随しませんので，手動でお願いします（なんとかしたい）．</mark>
</p>
<p>
  <%= link_to t(:new), new_charge_path, class: "link" %>
</p>

<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Staff</th>
        <th>Subject</th>
        <th>Capacity</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @charges.each do |charge| %>
        <tr>
          <td><%= charge.staff.lastname %></td>
          <td><%= charge.subject.name %></td>
          <td><%= charge.capacity %></td>
          <td><%= link_to t(:show), charge %></td>
          <td><%= link_to t(:edit), edit_charge_path(charge) %></td>
          <td><%= link_to t(:destroy), charge, method: :delete, data: { confirm: t(:sure) } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<p>
  <%= link_to t(:new), new_charge_path, class: "link" %>
</p>
